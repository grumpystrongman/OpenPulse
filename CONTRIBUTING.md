# Contributing

1. Create a feature branch from `main`.
2. Run `python -m ruff check .` and `python -m pytest` locally.
3. Include docs and tests for behavior changes.
4. For breaking changes, submit RFC using `docs/rfc-template.md` and governance review evidence.
