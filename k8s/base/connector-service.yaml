apiVersion: apps/v1
kind: Deployment
metadata:
  name: connector-service
  namespace: openpulse
spec:
  replicas: 1
  selector:
    matchLabels:
      app: connector-service
  template:
    metadata:
      labels:
        app: connector-service
    spec:
      containers:
        - name: connector-service
          image: openpulse/standard:0.1.0
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: openpulse-env
          workingDir: /app/services/connector-service
          command: ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port 8002"]
---
apiVersion: v1
kind: Service
metadata:
  name: connector-service
  namespace: openpulse
spec:
  selector:
    app: connector-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002

